import './globals.css';
export const metadata = { title: 'LeadNuvia — Turn conversations into customers', description: 'LeadNuvia is an AI sales assistant for website conversations, qualified leads, and bookings. Coming soon.' };
export default function RootLayout({children}:{children:React.ReactNode}){return <html lang="en"><body>{children}</body></html>}
